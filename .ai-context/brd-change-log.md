# BRD Change Log

No BRD has been ingested yet. This log will record each BRD revision (date, summary of change, affected requirements, and impacted specs) once `int-brd-ingestion` runs for the first time.
