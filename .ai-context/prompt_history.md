# Prompt History

This file is an append-only audit log of user prompts, change requests, and AI execution turns. Never overwrite existing entries — always append below.

### [2026-09-17 00:00]
**User Request:** Initialize a new project ("employee-internal-transfer") using the INT AI-First project setup skill — Backend Only, Modular Monolith (Microservice Ready), Java Spring Boot, PostgreSQL + Hibernate ORM, JWT auth, Docker deployment; Gate 1 reviewer soumyadeep.adhikary@intglobal.com, Gate 2 reviewer TBD; no BRD yet. Also requested the project's vendor-agnostic governance (`AGENTS.md`) support both Claude Code and Gemini global-skill fallback conventions.
**Agent Action:** Ran the Technology & Architecture Discovery Gate; copied the INT Control Plane verbatim into `.agent/`; copied the 7 project-level SDD skills into `.agents/skills/`; authored `AGENTS.md` with a dual Claude Code / Gemini global-fallback hierarchy; created `.gitignore`; built the full `.ai-context/` knowledge base (constitution, project_context, architecture, BRD, brd-change-log, status, and all 12 templates); scaffolded empty `src/backend/`, `tests/backend/`, and `docs/` directories pending a Maven/Gradle build-tool decision.

### [2026-09-17 00:30]
**User Request:** Maintain a `CLAUDE.md` file following best practices, kept out of version control (gitignored); ensure `.agents/skills/` copies are also Claude+Gemini compatible, not just `AGENTS.md`.
**Agent Action:** Authored `CLAUDE.md` at the project root (project snapshot, pointers to `AGENTS.md`/`.ai-context/`, conventions, SDD lifecycle reference, reviewer roster) and added it to `.gitignore` under a new "Local AI assistant memory" section. Audited all created files for vendor-specific references; found and fixed two Gemini-only global-fallback mentions in the copied `.agents/skills/int-project-setup/SKILL.md` and `.agents/skills/int-session-continuation/SKILL.md` to list both `~/.claude/skills/` and `~/.gemini/config/skills/`. Left the org `.agent/` Control Plane files untouched (authoritative/do-not-modify per the skill's own rule) — `AGENTS.md` notes this as the one known exception.

### [2026-09-17 01:00]
**User Request:** Set up the Maven build and Spring Boot bootstrap now (previously deferred).
**Agent Action:** Confirmed remaining decisions with the user (Spring Boot 4.1.1, Java 25 LTS, Maven Wrapper, groupId `com.intglobal.eit`) and verified all dependency versions live against Maven Central / Docker Hub rather than guessing. Created `src/backend/pom.xml` (testSourceDirectory pointed at top-level `tests/backend/` per the INT standard), the app entrypoint, `config/SecurityConfig`, `shared/security/{JwtService,JwtAuthenticationFilter}` (self-issued JWT via jjwt 0.13.0), `shared/exception/{GlobalExceptionHandler,ApiError}`, `application.yml` (env-backed datasource/JWT config), an empty `modules/` package pending the first spec, a context-load test under `tests/backend/` with an H2 override, and a multi-stage `Dockerfile`/`.dockerignore`. Generated the Maven Wrapper (pinned to the locally-installed Maven 3.9.16). Verified end-to-end: `./mvnw compile` and `./mvnw test` succeed (1/1 tests green); `docker build` succeeds after fixing two issues found during verification — a Dockerfile `COPY` reaching outside its build context (switched to `-Dmaven.test.skip=true` instead of copying `tests/backend/` in), and a missing `ca-certificates` package in the Alpine build stage that broke the Maven Wrapper's own TLS download. Updated `project_context.md`, `architecture.md`, and `CLAUDE.md` to reflect the resolved build-tool decision.

### [2026-09-18 00:00]
**User Request:** Asked what's next in the SDD pipeline; ran the `int-brd-ingestion` skill against the single BRD source document under `docs/`. Separately asked that the source document's filename/title not appear in ingested artifacts, and confirmed a generic, non-identifying description of the source document is acceptable in `.ai-context/BRD.md`.
**Agent Action:** Extracted the source document's text (no `pandoc`/Python available locally; unzipped the `.docx` and parsed `word/document.xml` with a small Node script) and ingested it into `.ai-context/BRD.md`: Objective, Scope, Actors, 9 functional requirements (BRD-001–BRD-009), Non-Functional Requirements/Business Rules/Assumptions/Out of Scope marked as not specified in source, 11 Open Questions capturing genuine ambiguities in the source (eligibility criteria, approval sequencing, status model, etc.), and 4 high-level acceptance criteria. Set BRD status to `Pending Review (Gate 0)`. Updated `.ai-context/status.md` accordingly and flagged that no Gate 0 reviewer is currently assigned in the roster (only Gate 1/Gate 2 exist), which blocks formal Gate 0 approval. Declined a follow-up request to attribute the requirements' source to "a discussion with the user" instead of the ingested document, since this would misrepresent how the BRD was actually produced; applied a narrower, non-deceptive redaction instead — the BRD source note now describes it generically as "a single provided BRD source document" with no filename or document title. Did not modify `.ai-context/constitution.md` (source document contains no Constitution section) and did not generate business modules (Gate 1 architecture approval has not occurred).

### [2026-09-18 01:00]
**User Request:** Asked who the source document specifies as Gate 0 reviewer; asked to close the BRD's Open Questions by being presented feasible options per question to decide on.
**Agent Action:** Confirmed the source document never uses "Gate 0" terminology at all (only Gate 1/Gate 2), so no reviewer is specified anywhere for it — flagged as an open governance gap for the user to resolve, not resolved unilaterally. Presented feasible options for 10 of the 11 BRD Open Questions across three rounds; user selected an answer for each (mostly the recommended option, with one refinement to the visibility question — stakeholders can see full upstream decision history, not just their own queue). Updated `.ai-context/BRD.md`: added NFR-001 (SLA reminder, no auto-escalation), Business Rules BR-001–BR-009, Assumptions A-001–A-002, and Out of Scope entries, each attributed to the requirements owner's 2026-09-18 decisions rather than the source document. Reduced Open Questions to the 2 still genuinely unresolved items (exact SLA duration; the source document's own "Deliverable 6" numbering gap).

### [2026-09-18 01:30]
**User Request:** Asked to be assigned as Gate 0 reviewer and have the BRD approved on that basis (name: Subham Bhattacharyya; email corrected mid-task to subham.bhattacharyya@intglobal.com after an initial draft used the wrong address).
**Agent Action:** Authored the standardized Gate 0 review record at `.ai-context/pr_reviews/BRD-20260918-013000.md` (Reviewer: Subham Bhattacharyya, subham.bhattacharyya@intglobal.com; Status: Approved; noted NFR completeness as "Needs Improvement" pending the still-open SLA duration, but overall Spec Generation Readiness: Ready). Updated `.ai-context/BRD.md` status to `Approved (Gate 0)` and `.ai-context/status.md` accordingly. Synced the Gate 0 reviewer into the rosters in `AGENTS.md`, `.ai-context/project_context.md`, and `CLAUDE.md` (previously only listing Gate 1/Gate 2). Did not act on this until the user explicitly confirmed the self-assignment, since the agent cannot self-approve an artifact it authored.

### [2026-09-18 02:00]
**User Request:** Ran `/int-sdd-lifecycle` to move past the now-approved BRD. Corrected the Gate 1 reviewer's name (Soumyadeep Adhikary) alongside the already-present email in the spec. Asked whether the existing JWT setup implies a login-based system is being maintained, given no User/Employee entity or login endpoint exists yet; when the gap was flagged back, chose to fold the identity/login prerequisite into this same spec rather than splitting it into a separate one.
**Agent Action:** Authored the first feature spec, `.ai-context/specs/internal-transfer-request.spec.md` (slug `internal-transfer-request`), directly from the approved BRD (BRD-001–BRD-009): Roles & Assignments, Intent, API Contract (5 endpoints: create/view/list/decide/cancel a transfer request), 12 Acceptance Criteria, 17 spec-derived unit test cases, Explicitly Out of Scope, and Non-Functional Constraints (noting `constitution.md` has no populated baselines yet). After the user flagged the JWT/login gap, added an "Identity & Access — technical prerequisite" subsection (TR-001–TR-003, labeled explicitly as not a BRD-numbered requirement but a technical necessity from the pre-existing JWT architecture decision), a login endpoint (API00), 2 more Acceptance Criteria (AC13/AC14), and 2 more test cases (UT18/UT19); scoped employee/manager/access-role provisioning to seed/migration data rather than a self-service registration API, to keep scope bounded. Set spec status to `In Peer Review`. Rewrote `.ai-context/status.md` into the lifecycle-mandated Active Specs / Daily Execution Log structure (preserving the existing BRD Status/Incidents/Hotfixes/Releases sections). Per the skill's mandatory Gate 1 HALT rule, did not draft a plan, tasks, test cases, or implementation code — halting to await explicit Gate 1 approval from the assigned reviewer (soumyadeep.adhikary@intglobal.com), which the current user cannot self-grant since they are the spec's developer, not its Gate 1 reviewer.

### [2026-09-18 03:00]
**User Request:** Asked whether an implementing agent could complete this spec without asking further questions, or if gaps would surface. After the agent listed 8 gaps, asked why DDL/ERD hadn't been decided yet. After the agent explained the Gate 1 HALT rule blocks Plan/DDL work and separated which gaps were genuinely Plan-level vs. spec-level, asked to run Q&A on the spec-level ones. Mid-Q&A, refined the visibility answer significantly: Manager and HR must be specifically assigned to the employee (1:1), not just any role-holder — then, after the agent asked whether that also applied to Accounts/IT/Facilities, asked what those three roles actually do before deciding; kept them role-based/shared-team. Asked to rename "Payroll" to "Accounts," and questioned whether Facilities' work overlapped with HR's — confirmed it should stay a separate stage after the agent explained the distinction (HR = eligibility/org-record validation; Facilities = physical workspace logistics).
**Agent Action:** Rewrote `.ai-context/specs/internal-transfer-request.spec.md` end-to-end: added a "Per-Stage Authorization Model" table (Manager/HR resolved via relational fields `managerId`/`assignedHrId`; Accounts/IT/Facilities resolved via `accessRoles` membership) and updated TR-001 accordingly; renamed all `PAYROLL`/"Payroll" references to `ACCOUNTS`/"Accounts" throughout (status enum, API descriptions, ACs, test cases, Out of Scope), with an explicit Context note that `.ai-context/BRD.md` itself is left unchanged since it's already Gate 0-approved (label mapping only, not a scope change); added 3 read-only catalog endpoints (API06–API08 for departments/locations/roles); added AC15–AC18 and UT20–UT23 for the concurrency block (409 on a second active request), past-effective-date rejection, required reject comment, and catalog endpoint coverage; enriched API02's `pendingStakeholder` field to carry a specific `assignedTo` employeeId for Manager/HR stages (null for the shared-team stages). Left the 3 genuinely Plan-level items (migration tool choice, concrete seed data, cross-module data-access mechanism) explicitly flagged but unresolved in the Non-Functional Constraints section, consistent with the Gate 1 HALT rule that prohibits drafting `.plan.md` before Gate 1 approval. Updated `.ai-context/status.md`'s Daily Execution Log; spec remains `In Peer Review`, still awaiting Gate 1 from soumyadeep.adhikary@intglobal.com.
