spring:
  datasource:
    url: jdbc:h2:mem:eit-test;MODE=PostgreSQL
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  jwt:
    secret: test-only-secret-key-not-for-production-use-0123456789
    expiration-ms: 3600000
